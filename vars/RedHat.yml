---
elasticsearch_installer: dlpkg
elasticsearch_pkg_mgr: rpm
elasticsearch_pkg : 'elasticsearch-{{elasticsearch_ver}}.rpm'
elasticsearch_pkg_url : '{{elasticsearch_mirror}}/rpm/elasticsearch/{{elasticsearch_ver}}/{{elasticsearch_pkg}}'
